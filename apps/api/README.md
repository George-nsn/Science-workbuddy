# Science Buddy API

FastAPI application and ARQ worker for the Science Buddy baseline. Install with `pip install -e ".[dev]"`, run migrations with `alembic upgrade head`, and start with `uvicorn science_buddy.main:app --reload`.
